diff --git a/submodules/ChatListUI/Sources/ChatListController.swift b/submodules/ChatListUI/Sources/ChatListController.swift
index 6c937d7a..132546d5 100644
--- a/submodules/ChatListUI/Sources/ChatListController.swift
+++ b/submodules/ChatListUI/Sources/ChatListController.swift
@@ -7282,6 +7282,7 @@ private final class ChatListLocationContext {
         presentationData: PresentationData
     ) {
         if stateAndFilterId.state.editing && stateAndFilterId.state.selectedThreadIds.count > 0 {
+            let hideChatListStatus = UserDefaults.standard.bool(forKey: "sosuzagram_hide_chat_list_status")
             self.chatTitleComponent = ChatTitleComponent(
                 context: self.context,
                 theme: presentationData.theme,
@@ -7307,7 +7308,7 @@ private final class ChatListLocationContext {
                 dateTimeFormat: presentationData.dateTimeFormat,
                 nameDisplayOrder: presentationData.nameDisplayOrder,
                 displayBackground: false,
-                content: .peer(peerView: ChatTitleContent.PeerData(peerView: peerView), customTitle: nil, customSubtitle: nil, onlineMemberCount: onlineMemberCount, isScheduledMessages: false, isMuted: nil, customMessageCount: nil, hidePeerStatus: false, isEnabled: true),
+                content: .peer(peerView: ChatTitleContent.PeerData(peerView: peerView), customTitle: nil, customSubtitle: nil, onlineMemberCount: onlineMemberCount, isScheduledMessages: false, isMuted: nil, customMessageCount: nil, hidePeerStatus: hideChatListStatus, isEnabled: true),
                 activities: nil,
                 networkState: nil,
                 tapped: { [weak self] in
diff --git a/submodules/Display/Source/HapticFeedback.swift b/submodules/Display/Source/HapticFeedback.swift
index 63b07ac5..a73f604b 100644
--- a/submodules/Display/Source/HapticFeedback.swift
+++ b/submodules/Display/Source/HapticFeedback.swift
@@ -3,6 +3,10 @@ import UIKit
 import AudioToolbox
 import CoreHaptics
 
+private func sosuzagramAppVibrationEnabled() -> Bool {
+    return UserDefaults.standard.object(forKey: "sosuzagram_app_vibration") as? Bool ?? true
+}
+
 public enum ImpactHapticFeedbackStyle: Hashable {
     case light
     case medium
@@ -132,6 +136,9 @@ public final class HapticFeedback {
     
     @available(iOSApplicationExtension 10.0, iOS 10.0, *)
     private func withImpl(_ f: (HapticFeedbackImpl) -> Void) {
+        guard sosuzagramAppVibrationEnabled() else {
+            return
+        }
         if self.impl == nil {
             self.impl = HapticFeedbackImpl()
         }
@@ -196,6 +203,9 @@ public final class HapticFeedback {
     
     public func warning() {
         if #available(iOSApplicationExtension 10.0, iOS 10.0, *) {
+            guard sosuzagramAppVibrationEnabled() else {
+                return
+            }
             self.withImpl { impl in
                 impl.warning()
             }
diff --git a/submodules/LocalizedPeerData/Sources/PeerTitle.swift b/submodules/LocalizedPeerData/Sources/PeerTitle.swift
index b30e6cab..eaf8e60d 100644
--- a/submodules/LocalizedPeerData/Sources/PeerTitle.swift
+++ b/submodules/LocalizedPeerData/Sources/PeerTitle.swift
@@ -4,23 +4,42 @@ import TelegramPresentationData
 import TelegramUIPreferences
 //import PhoneNumberFormat
 
+private let sosuzagramPeerTitleZalgoMarks = CharacterSet.nonBaseCharacters
+
+private func sosuzagramFilterPeerTitleIfNeeded(_ text: String) -> String {
+    guard UserDefaults.standard.bool(forKey: "sosuzagram_filter_zalgo"), !text.isEmpty else {
+        return text
+    }
+    
+    var filtered = String.UnicodeScalarView()
+    filtered.reserveCapacity(text.unicodeScalars.count)
+    
+    for scalar in text.unicodeScalars {
+        if !sosuzagramPeerTitleZalgoMarks.contains(scalar) {
+            filtered.append(scalar)
+        }
+    }
+    
+    return String(filtered)
+}
+
 public extension EnginePeer {
     var compactDisplayTitle: String {
         switch self {
         case let .user(user):
             if let firstName = user.firstName, !firstName.isEmpty {
-                return firstName
+                return sosuzagramFilterPeerTitleIfNeeded(firstName)
             } else if let lastName = user.lastName, !lastName.isEmpty {
-                return lastName
+                return sosuzagramFilterPeerTitleIfNeeded(lastName)
             } else if let _ = user.phone {
                 return "" //formatPhoneNumber("+\(phone)")
             } else {
                 return "Deleted Account"
             }
         case let .legacyGroup(group):
-            return group.title
+            return sosuzagramFilterPeerTitleIfNeeded(group.title)
         case let .channel(channel):
-            return channel.title
+            return sosuzagramFilterPeerTitleIfNeeded(channel.title)
         case .secretChat:
             return ""
         }
@@ -36,24 +55,24 @@ public extension EnginePeer {
                 if let lastName = user.lastName, !lastName.isEmpty {
                     switch displayOrder {
                     case .firstLast:
-                        return "\(firstName) \(lastName)"
+                        return sosuzagramFilterPeerTitleIfNeeded("\(firstName) \(lastName)")
                     case .lastFirst:
-                        return "\(lastName) \(firstName)"
+                        return sosuzagramFilterPeerTitleIfNeeded("\(lastName) \(firstName)")
                     }
                 } else {
-                    return firstName
+                    return sosuzagramFilterPeerTitleIfNeeded(firstName)
                 }
             } else if let lastName = user.lastName, !lastName.isEmpty {
-                return lastName
+                return sosuzagramFilterPeerTitleIfNeeded(lastName)
             } else if let _ = user.phone {
                 return "" //formatPhoneNumber("+\(phone)")
             } else {
                 return strings.User_DeletedAccount
             }
         case let .legacyGroup(group):
-            return group.title
+            return sosuzagramFilterPeerTitleIfNeeded(group.title)
         case let .channel(channel):
-            return channel.title
+            return sosuzagramFilterPeerTitleIfNeeded(channel.title)
         case .secretChat:
             return ""
         }
diff --git a/submodules/TelegramStringFormatting/Sources/MessageContentKind.swift b/submodules/TelegramStringFormatting/Sources/MessageContentKind.swift
index 86e0333e..75902261 100644
--- a/submodules/TelegramStringFormatting/Sources/MessageContentKind.swift
+++ b/submodules/TelegramStringFormatting/Sources/MessageContentKind.swift
@@ -264,8 +264,6 @@ public enum MessageContentKind: Equatable {
 }
 
 public func messageTextWithAttributes(message: EngineMessage) -> NSAttributedString {
-    var attributedText = NSAttributedString(string: message.text)
-    
     var entities: TextEntitiesMessageAttribute?
     for attribute in message.attributes {
         if let attribute = attribute as? TextEntitiesMessageAttribute {
@@ -273,10 +271,14 @@ public func messageTextWithAttributes(message: EngineMessage) -> NSAttributedStr
             break
         }
     }
-    if let entities = entities?.entities {
+    
+    let filteredMessage = sosuzagramFilterZalgoIfNeeded(text: message.text, entities: entities?.entities ?? [])
+    var attributedText = NSAttributedString(string: filteredMessage.0)
+    
+    if !filteredMessage.1.isEmpty {
         let updatedString = NSMutableAttributedString(attributedString: attributedText)
         
-        for entity in entities.sorted(by: { $0.range.lowerBound > $1.range.lowerBound }) {
+        for entity in filteredMessage.1.sorted(by: { $0.range.lowerBound > $1.range.lowerBound }) {
             guard case let .CustomEmoji(_, fileId) = entity.type else {
                 continue
             }
@@ -455,7 +457,7 @@ public func stringForMediaKind(_ kind: MessageContentKind, strings: Presentation
         if text.isEmpty {
             return (NSAttributedString(string: strings.Message_Sticker), true)
         } else {
-            return (NSAttributedString(string: strings.Message_StickerText(text).string), true)
+            return (NSAttributedString(string: strings.Message_StickerText(sosuzagramFilterZalgoIfNeeded(text)).string), true)
         }
     case .animation:
         return (NSAttributedString(string: strings.Message_Animation), true)
@@ -463,12 +465,12 @@ public func stringForMediaKind(_ kind: MessageContentKind, strings: Presentation
         if text.isEmpty {
             return (NSAttributedString(string: strings.Message_File), true)
         } else {
-            return (NSAttributedString(string: text), true)
+            return (NSAttributedString(string: sosuzagramFilterZalgoIfNeeded(text)), true)
         }
     case .contact:
         return (NSAttributedString(string: strings.Message_Contact), true)
     case let .game(text):
-        return (NSAttributedString(string: text), true)
+        return (NSAttributedString(string: sosuzagramFilterZalgoIfNeeded(text)), true)
     case .location:
         return (NSAttributedString(string: strings.Message_Location), true)
     case .liveLocation:
@@ -482,15 +484,15 @@ public func stringForMediaKind(_ kind: MessageContentKind, strings: Presentation
     case .expiredVideoMessage:
         return (NSAttributedString(string: strings.Message_VideoMessageExpired), true)
     case let .poll(text):
-        return (NSAttributedString(string: text), false)
+        return (NSAttributedString(string: sosuzagramFilterZalgoIfNeeded(text)), false)
     case let .todo(text):
-        return (NSAttributedString(string: "☑️ \(text)"), false)
+        return (NSAttributedString(string: "☑️ \(sosuzagramFilterZalgoIfNeeded(text))"), false)
     case let .restricted(text):
-        return (NSAttributedString(string: text), false)
+        return (NSAttributedString(string: sosuzagramFilterZalgoIfNeeded(text)), false)
     case let .dice(emoji):
         return (NSAttributedString(string: emoji), true)
     case let .invoice(text):
-        return (NSAttributedString(string: text), true)
+        return (NSAttributedString(string: sosuzagramFilterZalgoIfNeeded(text)), true)
     case .story:
         return (NSAttributedString(string: strings.Message_Story), true)
     case .giveaway:
@@ -521,7 +523,7 @@ public func foldLineBreaks(_ text: String) -> String {
         }
     }
     result = result.replacingOccurrences(of: "\\s+", with: " ", options: .regularExpression)
-    return result
+    return sosuzagramFilterZalgoIfNeeded(result)
 }
 
 public func foldLineBreaks(_ text: NSAttributedString) -> NSAttributedString {
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoData.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoData.swift
index dea0dfbc..9dee6b55 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoData.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoData.swift
@@ -27,6 +27,58 @@ enum AvatarUploadProgress {
     case indefinite
 }
 
+private func sosuzagramRelativeOnlineTime() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_relative_online_time")
+}
+
+private func sosuzagramPreciseLastSeenString(strings: PresentationStrings, dateTimeFormat: PresentationDateTimeFormat, timestamp: Int32, relativeTo currentTimestamp: Int32) -> String {
+    var t: time_t = time_t(timestamp)
+    var timeinfo: tm = tm()
+    localtime_r(&t, &timeinfo)
+    
+    let timeString = stringForShortTimestamp(hours: timeinfo.tm_hour, minutes: timeinfo.tm_min, dateTimeFormat: dateTimeFormat)
+    
+    var now: time_t = time_t(currentTimestamp)
+    var timeinfoNow: tm = tm()
+    localtime_r(&now, &timeinfoNow)
+    
+    if timeinfo.tm_year != timeinfoNow.tm_year {
+        let dateString = stringForTimestamp(day: timeinfo.tm_mday, month: timeinfo.tm_mon + 1, year: timeinfo.tm_year, dateTimeFormat: dateTimeFormat)
+        return strings.LastSeen_AtDate("\(dateString), \(timeString)").string
+    }
+    
+    let dayDifference = timeinfo.tm_yday - timeinfoNow.tm_yday
+    if dayDifference == 0 {
+        return strings.LastSeen_TodayAt(timeString).string
+    } else if dayDifference == -1 {
+        return strings.LastSeen_YesterdayAt(timeString).string
+    } else {
+        let dateString = stringForTimestamp(day: timeinfo.tm_mday, month: timeinfo.tm_mon + 1, year: timeinfo.tm_year, dateTimeFormat: dateTimeFormat)
+        return strings.LastSeen_AtDate("\(dateString), \(timeString)").string
+    }
+}
+
+private func sosuzagramPresenceText(strings: PresentationStrings, dateTimeFormat: PresentationDateTimeFormat, presence: TelegramUserPresence, relativeTo timestamp: Int32) -> (String, Bool) {
+    if !sosuzagramRelativeOnlineTime() {
+        return stringAndActivityForUserPresence(strings: strings, dateTimeFormat: dateTimeFormat, presence: EnginePeer.Presence(presence), relativeTo: timestamp, expanded: true)
+    }
+    
+    switch presence.status {
+    case let .present(statusTimestamp):
+        if statusTimestamp >= timestamp {
+            return (strings.Presence_online, true)
+        } else {
+            return (sosuzagramPreciseLastSeenString(strings: strings, dateTimeFormat: dateTimeFormat, timestamp: statusTimestamp, relativeTo: timestamp), false)
+        }
+    case .none, .recently, .lastWeek, .lastMonth:
+        if presence.lastActivity > 0 {
+            return (sosuzagramPreciseLastSeenString(strings: strings, dateTimeFormat: dateTimeFormat, timestamp: presence.lastActivity, relativeTo: timestamp), false)
+        } else {
+            return stringAndActivityForUserPresence(strings: strings, dateTimeFormat: dateTimeFormat, presence: EnginePeer.Presence(presence), relativeTo: timestamp, expanded: true)
+        }
+    }
+}
+
 final class PeerInfoState {
     let isEditing: Bool
     let selectedMessageIds: Set<MessageId>?
@@ -1193,7 +1245,7 @@ func peerInfoScreenData(
                     let data = manager.with { manager -> PeerInfoStatusData? in
                         if let presence = manager.currentValue {
                             let timestamp = CFAbsoluteTimeGetCurrent() + NSTimeIntervalSince1970
-                            let (text, isActivity) = stringAndActivityForUserPresence(strings: strings, dateTimeFormat: dateTimeFormat, presence: EnginePeer.Presence(presence), relativeTo: Int32(timestamp), expanded: true)
+                            let (text, isActivity) = sosuzagramPresenceText(strings: strings, dateTimeFormat: dateTimeFormat, presence: presence, relativeTo: Int32(timestamp))
                             var isHiddenStatus = false
                             switch presence.status {
                             case .recently(let isHidden), .lastWeek(let isHidden), .lastMonth(let isHidden):
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoHeaderNode.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoHeaderNode.swift
index e7676649..4f723366 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoHeaderNode.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoHeaderNode.swift
@@ -46,6 +46,10 @@ import BundleIconComponent
 import MarqueeComponent
 import EdgeEffect
 
+private func sosuzagramHidePhoneNumberInPeerInfo() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_hide_phone_number")
+}
+
 final class PeerInfoHeaderNavigationTransition {
     let sourceNavigationBar: NavigationBar
     let sourceTitleView: ChatTitleView
@@ -1222,7 +1226,7 @@ final class PeerInfoHeaderNode: ASDisplayNode {
                 title = "" //"\u{00A0}"
             }
             if title.isEmpty {
-                if case let .user(user) = peer, let phone = user.phone {
+                if case let .user(user) = peer, let phone = user.phone, !sosuzagramHidePhoneNumberInPeerInfo() {
                     title = formatPhoneNumber(context: self.context, number: phone)
                 } else if let addressName = peer.addressName {
                     title = "@\(addressName)"
@@ -1236,10 +1240,13 @@ final class PeerInfoHeaderNode: ASDisplayNode {
             smallTitleAttributes = MultiScaleTextState.Attributes(font: Font.medium(28.0), color: .white, shadowColor: titleShadowColor)
             
             if self.isSettings, case let .user(user) = peer {
-                var subtitle = formatPhoneNumber(context: self.context, number: user.phone ?? "")
+                var subtitle = sosuzagramHidePhoneNumberInPeerInfo() ? "" : formatPhoneNumber(context: self.context, number: user.phone ?? "")
                 
                 if let mainUsername = user.addressName, !mainUsername.isEmpty {
-                    subtitle = "\(subtitle) • @\(mainUsername)"
+                    subtitle = subtitle.isEmpty ? "@\(mainUsername)" : "\(subtitle) / @\(mainUsername)"
+                }
+                if sosuzagramHidePhoneNumberInPeerInfo(), let mainUsername = user.addressName, !mainUsername.isEmpty {
+                    subtitle = "@\(mainUsername)"
                 }
                 subtitleStringText = subtitle
                 subtitleAttributes = MultiScaleTextState.Attributes(font: Font.regular(17.0), color: .white)
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoProfileItems.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoProfileItems.swift
index be77e082..c564979d 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoProfileItems.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoProfileItems.swift
@@ -18,6 +18,14 @@ import AvatarNode
 import PeerNameColorItem
 import BoostLevelIconComponent
 
+private func sosuzagramHidePhoneNumberInProfile() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_hide_phone_number")
+}
+
+private func sosuzagramShowIdAndDcInProfile() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_show_id_dc")
+}
+
 private let enabledPublicBioEntities: EnabledEntityTypes = [.allUrl, .mention, .hashtag]
 private let enabledPrivateBioEntities: EnabledEntityTypes = [.internalUrl, .mention, .hashtag]
 
@@ -138,7 +146,7 @@ func infoItems(
             }))
         }
         
-        if let phone = user.phone {
+        if let phone = user.phone, !sosuzagramHidePhoneNumberInProfile() {
             let formattedPhone = formatPhoneNumber(context: context, number: phone)
             let label: String
             if formattedPhone.hasPrefix("+888 ") {
@@ -154,6 +162,22 @@ func infoItems(
                 interaction.requestLayout(animated)
             }))
         }
+        if sosuzagramShowIdAndDcInProfile() {
+            let peerIdValue = user.id.id._internalGetInt64Value()
+            let dcId = Int32(context.account.network.datacenterId)
+            items[currentPeerInfoSection]!.append(
+                PeerInfoScreenLabeledValueItem(
+                    id: ItemUsername + 10_000,
+                    label: "ID / DC",
+                    text: "\(peerIdValue) / DC\(dcId)",
+                    textColor: .primary,
+                    action: nil,
+                    requestLayout: { animated in
+                        interaction.requestLayout(animated)
+                    }
+                )
+            )
+        }
         if let mainUsername = user.addressName {
             var additionalUsernames: String?
             let usernames = user.usernames.filter { $0.isActive && $0.username != mainUsername }
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
index 32aa2f66..cad6ad68 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
@@ -1,3 +1,4 @@
+import SosuzagramIOSCore
 import Foundation
 import UIKit
 import Display
@@ -14,6 +15,14 @@ import DeviceAccess
 import TelegramStringFormatting
 import PeerNameColorItem
 
+private func sosuzagramHidePhoneNumberInSettings() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_hide_phone_number")
+}
+
+private func sosuzagramShowIdAndDcInSettings() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_show_id_dc")
+}
+
 enum SettingsSection: Int, CaseIterable {
     case edit
     case phone
@@ -86,7 +95,7 @@ func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentat
             items[.phone]!.append(PeerInfoScreenActionItem(id: 1, text: "Restore Subscription", action: {
                 interaction.openSettings(.premiumManagement)
             }))
-        } else if settings.suggestPhoneNumberConfirmation, case let .user(peer) = data.peer {
+        } else if settings.suggestPhoneNumberConfirmation, case let .user(peer) = data.peer, !sosuzagramHidePhoneNumberInSettings() {
             let phoneNumber = formatPhoneNumber(context: context, number: peer.phone ?? "")
             items[.phone]!.append(PeerInfoScreenInfoItem(id: 0, title: presentationData.strings.Settings_CheckPhoneNumberTitle(phoneNumber).string, text: .markdown(presentationData.strings.Settings_CheckPhoneNumberText), linkAction: { link in
                 if case .tap = link {
@@ -234,6 +243,9 @@ func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentat
     items[.advanced]!.append(PeerInfoScreenDisclosureItem(id: 1, text: presentationData.strings.Settings_PrivacySettings, icon: PresentationResourcesSettings.security, action: {
         interaction.openSettings(.privacyAndSecurity)
     }))
+    items[.advanced]!.append(PeerInfoScreenDisclosureItem(id: 100, text: "Настройки Sosuzagram", icon: PresentationResourcesSettings.security, action: {
+        interaction.openSettings(.sosuzagramSettings)
+    }))
     items[.advanced]!.append(PeerInfoScreenDisclosureItem(id: 2, text: presentationData.strings.Settings_ChatSettings, icon: PresentationResourcesSettings.dataAndStorage, action: {
         interaction.openSettings(.dataAndStorage)
     }))
@@ -444,11 +456,17 @@ func settingsEditingItems(data: PeerInfoScreenData?, state: PeerInfoState, conte
         }))
     }
     
-    if case let .user(user) = data.peer {
+    if case let .user(user) = data.peer, !sosuzagramHidePhoneNumberInSettings() {
         items[.info]!.append(PeerInfoScreenDisclosureItem(id: ItemPhoneNumber, label: .text(user.phone.flatMap({ formatPhoneNumber(context: context, number: $0) }) ?? ""), text: presentationData.strings.Settings_PhoneNumber, icon: PresentationResourcesSettings.recentCalls, action: {
             interaction.openSettings(.phoneNumber)
         }))
     }
+    if sosuzagramShowIdAndDcInSettings(), let peer = data.peer {
+        let peerIdValue = peer.id.id._internalGetInt64Value()
+        let dcId = Int32(context.account.network.datacenterId)
+        items[.info]!.append(PeerInfoScreenDisclosureItem(id: ItemPhoneNumber + 10_000, label: .text("\(peerIdValue) / DC\(dcId)"), text: "ID / DC", icon: PresentationResourcesSettings.devices, action: {
+        }))
+    }
     var username = ""
     if let addressName = data.peer?.addressName, !addressName.isEmpty {
         username = "@\(addressName)"
diff --git a/submodules/TelegramUI/Sources/ServiceSoundManager.swift b/submodules/TelegramUI/Sources/ServiceSoundManager.swift
index 363c12ec..5be2705f 100644
--- a/submodules/TelegramUI/Sources/ServiceSoundManager.swift
+++ b/submodules/TelegramUI/Sources/ServiceSoundManager.swift
@@ -3,6 +3,10 @@ import SwiftSignalKit
 import AudioToolbox
 import AppBundle
 
+private func sosuzagramAppVibrationEnabled() -> Bool {
+    return UserDefaults.standard.object(forKey: "sosuzagram_app_vibration") as? Bool ?? true
+}
+
 private func loadSystemSoundFromBundle(name: String) -> SystemSoundID? {
     let path = "\(getAppBundle().resourcePath!)/\(name)"
     let url = URL(fileURLWithPath: path)
@@ -43,6 +47,9 @@ public final class ServiceSoundManager {
     
     public func playVibrationSound() {
         self.queue.async {
+            guard sosuzagramAppVibrationEnabled() else {
+                return
+            }
             AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
         }
     }
diff --git a/submodules/TextFormat/Sources/StringWithAppliedEntities.swift b/submodules/TextFormat/Sources/StringWithAppliedEntities.swift
index 29234377..7899acca 100644
--- a/submodules/TextFormat/Sources/StringWithAppliedEntities.swift
+++ b/submodules/TextFormat/Sources/StringWithAppliedEntities.swift
@@ -7,6 +7,71 @@ import libprisma
 import SwiftSignalKit
 import TelegramPresentationData
 
+private let sosuzagramZalgoMarks = CharacterSet.nonBaseCharacters
+
+private func sosuzagramFilterZalgoEnabled() -> Bool {
+    return UserDefaults.standard.bool(forKey: "sosuzagram_filter_zalgo")
+}
+
+public func sosuzagramFilterZalgoIfNeeded(_ text: String) -> String {
+    guard sosuzagramFilterZalgoEnabled(), !text.isEmpty else {
+        return text
+    }
+    
+    var filtered = String.UnicodeScalarView()
+    filtered.reserveCapacity(text.unicodeScalars.count)
+    
+    for scalar in text.unicodeScalars {
+        if !sosuzagramZalgoMarks.contains(scalar) {
+            filtered.append(scalar)
+        }
+    }
+    
+    return String(filtered)
+}
+
+public func sosuzagramFilterZalgoIfNeeded(text: String, entities: [MessageTextEntity]) -> (String, [MessageTextEntity]) {
+    guard sosuzagramFilterZalgoEnabled(), !text.isEmpty else {
+        return (text, entities)
+    }
+    
+    var filtered = String.UnicodeScalarView()
+    filtered.reserveCapacity(text.unicodeScalars.count)
+    
+    var offsetMap: [Int: Int] = [0: 0]
+    var originalOffset = 0
+    var filteredOffset = 0
+    
+    for scalar in text.unicodeScalars {
+        let scalarLength = scalar.utf16.count
+        offsetMap[originalOffset] = filteredOffset
+        
+        if !sosuzagramZalgoMarks.contains(scalar) {
+            filtered.append(scalar)
+            filteredOffset += scalarLength
+        }
+        
+        originalOffset += scalarLength
+        offsetMap[originalOffset] = filteredOffset
+    }
+    
+    let filteredText = String(filtered)
+    if filteredText == text {
+        return (text, entities)
+    }
+    
+    let filteredEntities = entities.compactMap { entity -> MessageTextEntity? in
+        let lowerBound = offsetMap[entity.range.lowerBound] ?? entity.range.lowerBound
+        let upperBound = offsetMap[entity.range.upperBound] ?? entity.range.upperBound
+        guard upperBound > lowerBound else {
+            return nil
+        }
+        return MessageTextEntity(range: lowerBound ..< upperBound, type: entity.type)
+    }
+    
+    return (filteredText, filteredEntities)
+}
+
 public func chatInputStateStringWithAppliedEntities(_ text: String, entities: [MessageTextEntity]) -> NSAttributedString {
     var nsString: NSString?
     let string = NSMutableAttributedString(string: text)
@@ -128,6 +193,9 @@ private func generateMessageSyntaxHighlight(spec: CachedMessageSyntaxHighlight.S
 }
 
 public func stringWithAppliedEntities(_ text: String, entities: [MessageTextEntity], strings: PresentationStrings? = nil, dateTimeFormat: PresentationDateTimeFormat? = nil, baseColor: UIColor, linkColor: UIColor, baseQuoteTintColor: UIColor? = nil, baseQuoteSecondaryTintColor: UIColor? = nil, baseQuoteTertiaryTintColor: UIColor? = nil, codeBlockTitleColor: UIColor? = nil, codeBlockAccentColor: UIColor? = nil, codeBlockBackgroundColor: UIColor? = nil, baseFont: UIFont, linkFont: UIFont, boldFont: UIFont, italicFont: UIFont, boldItalicFont: UIFont, fixedFont: UIFont, blockQuoteFont: UIFont, underlineLinks: Bool = true, external: Bool = false, message: Message?, entityFiles: [MediaId: TelegramMediaFile] = [:], adjustQuoteFontSize: Bool = false, cachedMessageSyntaxHighlight: CachedMessageSyntaxHighlight? = nil, paragraphAlignment: NSTextAlignment? = nil) -> NSAttributedString {
+    let filteredInput = sosuzagramFilterZalgoIfNeeded(text: text, entities: entities)
+    let text = filteredInput.0
+    let entities = filteredInput.1
     let baseQuoteTintColor = baseQuoteTintColor ?? baseColor
     
     var nsString: NSString?
